using UnityEngine;
using System.Collections;
using System.Collections.Generic;
public class CustomRaycast : MonoBehaviour
{

    private const float STEPS = 50f;

    public static CustomCollider Raycast(Vector3 from, Vector3 to)
    {
        Vector3 rayDirection = to - from;
        return null;
    }
}
